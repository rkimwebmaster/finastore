{% extends 'base.html.twig' %}

{% block title %}Reinitialiser mot de pase {% endblock %}

{% block body %}
    <h1>Reinitailisation de mot de passe </h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Reinitailise le mot de passe </button>
    {{ form_end(resetForm) }}
{% endblock %}
