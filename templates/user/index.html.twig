{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs {% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Identité</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.rolePrincipal }}</td>
                <td>{{ user.identite }}</td>
                <td>{{ user.adresse }}</td>
                <td>
                    <div class="btn-group">
                    <a class="btn btn-sm btn-default" href="{{ path('app_user_show', {'id': user.id}) }}">voir</a>
                    <a  class="btn btn-sm  btn-default" href="{{ path('app_user_edit', {'id': user.id}) }}">modifier</a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('app_register') }}"> Nouvel utilisateur </a>
    <a class="btn btn-success" href="{{ path('app_register_admin') }}"> Nouvel admin </a>
{% endblock %}


